[toolchain]
# Track `stable` (same as CI, which uses dtolnay/rust-toolchain@stable) so local
# fmt/clippy match CI exactly. Bump/lock here if you need a frozen toolchain.
channel = "stable"
components = ["clippy", "rustfmt", "rust-src"]
